import React from 'react';
import ProfileScreen from '../../screens/ProfileScreen/ProfileScreen';

const ProfileContainer = () => {
  // Lógica de perfil pode ser adicionada aqui
  return <ProfileScreen />;
};

export default ProfileContainer;
